SELECT
    employeeNumber,
    firstName,
    lastName,
    hire_date,
    salary
FROM classicmodels.employees
ORDER BY firstName;

SELECT
    employeeNumber,
    firstName,
    lastName,
    hire_date,
    salary
FROM classicmodels.employees
ORDER BY salary DESC;
